@model BookShelf.Data.Models.Book
<div class="card mb-2">
    <div class="card-body">
        <h5 class="card-title mb-1">Title: @Model.Title</h5>
        <div class="text-muted">

            Year: (@Model.Year • Author: @(Model.Author.Name ?? "Unknown")
        </div>
    </div>
</div>
